import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { trpc } from "@/lib/trpc";
import { Check, UserPlus, Users, X, Trash2 } from "lucide-react";
import { useEffect, useState } from "react";
import { toast } from "sonner";

function errorText(error: { message?: string }) { return error.message || "Something went wrong."; }

export default function PlayerManagement() {
  const leagues = trpc.league.overview.useQuery(undefined, { refetchOnWindowFocus: false });
  const [leagueId, setLeagueId] = useState("");
  useEffect(() => { if (!leagueId && leagues.data?.[0]) setLeagueId(String(leagues.data[0].id)); }, [leagueId, leagues.data]);
  const activeLeagueId = Number(leagueId) || 0;
  const dashboard = trpc.league.dashboard.useQuery({ leagueId: activeLeagueId }, { enabled: activeLeagueId > 0, refetchOnWindowFocus: false });
  const players = trpc.playerAdmin.list.useQuery({ leagueId: activeLeagueId }, { enabled: activeLeagueId > 0, refetchOnWindowFocus: false });
  const refresh = async () => { await Promise.all([players.refetch(), dashboard.refetch(), leagues.refetch()]); };
  const add = trpc.playerAdmin.add.useMutation({ onSuccess: async () => { toast.success("Player registration added for review."); setForm({ email: "", playerName: "", username: "", whatsappNumber: "" }); await refresh(); }, onError: (error) => toast.error(errorText(error)) });
  const review = trpc.playerAdmin.review.useMutation({ onSuccess: async () => { toast.success("Player registration updated."); await refresh(); }, onError: (error) => toast.error(errorText(error)) });
  const remove = trpc.playerAdmin.remove.useMutation({ onSuccess: async () => { toast.success("Player removed from this league."); await refresh(); }, onError: (error) => toast.error(errorText(error)) });
  const [form, setForm] = useState({ email: "", playerName: "", username: "", whatsappNumber: "" });
  const teamOptions = dashboard.data?.teams ?? [];
  const selectedLeague = leagues.data?.find((league) => league.id === activeLeagueId);
  return <div className="mx-auto max-w-[1280px] px-4 py-6 sm:px-8 sm:py-10"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-[11px] font-bold uppercase tracking-[0.2em] text-primary">Player management</p><h1 className="mt-2 text-3xl font-bold tracking-tight">Build the player register.</h1><p className="mt-2 text-sm text-muted-foreground">Only the owner of a league can approve registrations or change team assignments.</p></div><div className="w-full sm:w-72"><Label>Active league</Label><Select value={leagueId} onValueChange={setLeagueId}><SelectTrigger className="mt-2 h-11 rounded-xl"><SelectValue placeholder="Choose a league" /></SelectTrigger><SelectContent>{leagues.data?.map((league) => <SelectItem key={league.id} value={String(league.id)}>{league.name} · {league.seasonName}</SelectItem>)}</SelectContent></Select></div></div>{!selectedLeague ? <Card className="mt-8 rounded-[22px] shadow-none"><CardContent className="grid place-items-center gap-3 p-12 text-center"><Users className="size-9 text-primary" /><h2 className="text-xl font-bold">Create a league first</h2><p className="text-sm text-muted-foreground">Player registrations are always scoped to one of your leagues.</p></CardContent></Card> : <div className="mt-8 grid gap-6 xl:grid-cols-[360px_minmax(0,1fr)]"><Card className="rounded-[22px] border-border/80 shadow-none"><CardHeader><CardTitle className="flex items-center gap-2"><UserPlus className="size-5 text-primary" /> Add a player</CardTitle><CardDescription>The player must already have an account. Use the email tied to their OAuth account.</CardDescription></CardHeader><CardContent><form onSubmit={(event) => { event.preventDefault(); add.mutate({ leagueId: activeLeagueId, ...form }); }} className="grid gap-4"><div><Label>Account email</Label><Input required type="email" value={form.email} onChange={(event) => setForm({ ...form, email: event.target.value })} placeholder="player@example.com" /></div><div><Label>Player name</Label><Input required value={form.playerName} onChange={(event) => setForm({ ...form, playerName: event.target.value })} /></div><div><Label>Username</Label><Input required value={form.username} onChange={(event) => setForm({ ...form, username: event.target.value })} placeholder="mvp_player" /></div><div><Label>WhatsApp number <span className="font-normal text-muted-foreground">optional</span></Label><Input value={form.whatsappNumber} onChange={(event) => setForm({ ...form, whatsappNumber: event.target.value })} /></div><Button disabled={add.isPending} className="h-11 rounded-xl font-bold">{add.isPending ? "Adding…" : "Add for approval"}</Button></form></CardContent></Card><Card className="rounded-[22px] border-border/80 shadow-none"><CardHeader className="border-b border-border/70"><CardTitle>Registered players</CardTitle><CardDescription>{players.data?.length ?? 0} player registration{players.data?.length === 1 ? "" : "s"} in {selectedLeague.name}.</CardDescription></CardHeader><CardContent className="grid gap-3 p-5">{players.data?.length ? players.data.map(({ membership, user, team }) => <div key={membership.id} className="grid gap-4 rounded-2xl border border-border/70 p-4 lg:grid-cols-[minmax(180px,1fr)_170px_auto]"><div className="min-w-0"><div className="flex items-center gap-2"><p className="font-bold">{membership.playerName}</p><Badge variant={membership.registrationStatus === "approved" ? "default" : membership.registrationStatus === "rejected" ? "destructive" : "secondary"} className="rounded-full capitalize">{membership.registrationStatus}</Badge></div><p className="mt-1 truncate text-sm text-muted-foreground">@{membership.username} · {user.email || "No email"}</p></div><Select value={team?.id ? String(team.id) : "unassigned"} onValueChange={(value) => review.mutate({ membershipId: membership.id, registrationStatus: "approved", teamId: value === "unassigned" ? null : Number(value) })}><SelectTrigger className="h-10 rounded-xl"><SelectValue placeholder="Assign team" /></SelectTrigger><SelectContent><SelectItem value="unassigned">No team</SelectItem>{teamOptions.map((option) => <SelectItem key={option.id} value={String(option.id)}>{option.name}</SelectItem>)}</SelectContent></Select><div className="flex items-center justify-end gap-2">{membership.registrationStatus !== "approved" && <Button size="sm" className="rounded-lg" onClick={() => review.mutate({ membershipId: membership.id, registrationStatus: "approved", teamId: team?.id ?? null })}><Check className="size-4" /> Approve</Button>}{membership.registrationStatus !== "rejected" && <Button size="sm" variant="outline" className="rounded-lg" onClick={() => review.mutate({ membershipId: membership.id, registrationStatus: "rejected", teamId: null })}><X className="size-4" /> Reject</Button>}<Button size="icon-sm" variant="ghost" className="text-destructive hover:bg-destructive/10 hover:text-destructive" onClick={() => remove.mutate({ membershipId: membership.id })} aria-label={`Remove ${membership.playerName}`}><Trash2 className="size-4" /></Button></div></div>) : <div className="rounded-xl bg-muted/50 p-8 text-center text-sm text-muted-foreground">No players registered yet. Add a player account to start the register.</div>}</CardContent></Card></div>}</div>;
}
